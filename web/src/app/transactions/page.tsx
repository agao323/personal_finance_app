import { EmptyState } from "@/components/states";

export default function TransactionsPage() {
  return (
    <EmptyState
      title="Transactions land in ticket 030"
      detail="The route exists so navigation is never a dead link while the lane is in flight."
    />
  );
}
